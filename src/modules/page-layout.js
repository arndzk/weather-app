const buildHeader = () => {};

const buildMain = () => {};

const buildFooter = () => {};

const buildPageLayout = () => {
  buildHeader();
  buildMain();
  buildFooter();
};

export default buildPageLayout;
